git init
git status
git add .
//first time
git config --global user.name "kritika jain"
git config --global user.email "git mail"
git commit -m "you message"

#first time
git remote remove origin
git remote add origin "https://github.com/kritikajain

#daily commands
git add.
git commit -m "daily comment"
git push origin master